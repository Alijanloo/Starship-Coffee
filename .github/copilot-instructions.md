## Instructions

* Use Pydantic models for all entities to ensure type safety, automatic validation, and consistent serialization/deserialization.

* Use the `uv` package manager:
```
uv pip list
uv pip install ...
uv run python <file.py>
```

## Notes

* No Inline comments at all, just the important doc strings
* Put application-wide constants in the `.env` file.
